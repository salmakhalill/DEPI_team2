{
  "vulnerability": "Missing Rate Limiting",
  "cases": [
    {
      "id": "rate-limit-login-01",
      "payload": "wrongpassword",
      "match_regex": "(?i)(invalid|incorrect|wrong password)",
      "description": "Repeated failed login attempts with no lockout or throttling mechanism."
    }
  ]
}