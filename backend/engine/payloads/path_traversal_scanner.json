{
  "vulnerability": "Path Traversal",
  "cases": [
    {
      "id": "pt-unix-basic",
      "payload": "../../../../etc/passwd",
      "match_regex": "(?i)(root:x:0:0|daemon:|nobody:|/bin/bash|/bin/sh)",
      "description": "Classic Unix path traversal targeting /etc/passwd to confirm file read via root or system user entries."
    },
    {
      "id": "pt-win-basic",
      "payload": "..\\..\\..\\..\\windows\\win.ini",
      "match_regex": "(?i)(\\[fonts\\]|\\[extensions\\]|\\[mci extensions\\]|for 16-bit app support)",
      "description": "Windows path traversal targeting win.ini to confirm file read via known section headers."
    }
  ]
}